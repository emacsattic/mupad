------------------------------------------------------------------------------
Take care of tar files in the debugger

------------------------------------------------------------------------------
When a history search fails, restart from the end of the history, or
at least let the next search start from the end of the history. (done)

------------------------------------------------------------------------------
Allow for looking up ascii documentation in several directories

------------------------------------------------------------------------------
Should mupad-run get dynamically the version, proper flags and the path
to the help ? not quite sure

------------------------------------------------------------------------------
How to avoid the recentering ? That is, except at the very beginning,
I would like the bottom of the buffer to be displayed at the bottom of
the window. (improved; test if the heuristic is ok!)

------------------------------------------------------------------------------
Allow for deleting blocs of input/output

------------------------------------------------------------------------------
When fetching a multiline command from the history, scroll down so
that one can see the full command ? (done)

------------------------------------------------------------------------------
Turn mupad-mode on on /tmp/debug.**** (done+avoid reloading the file)

------------------------------------------------------------------------------
Add a init.mu file to the MuPAD directory for vcam ?
Ou demander `a mupad-bus d'envoyer les commandes qui vont bien à MuPAD ?
